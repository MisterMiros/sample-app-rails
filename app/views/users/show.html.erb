<%= provide(:title, @user.name) %>
<aside class="row">
  <div class="col-sm-4 col-md-4">
    <section>
      <h1>
        <%= gravatar_for @user, size: 40 %>
        <%= @user.name %>
      </h1>
    </section>
  </div>
</aside>